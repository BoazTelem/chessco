// Drizzle schema + migrations live here.
// Phase 0 Week 2: populate full schema per spec §5.
// Phase 0 stub for now.

export const schemaVersion = '0.0.0' as const;

export type SchemaVersion = typeof schemaVersion;
